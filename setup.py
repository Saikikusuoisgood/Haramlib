from setuptools import setup

setup(
   name='Haramlib',
   version='1.0',
   description='No more haram python coding',
   author='saikiki',
   author_email='',
   packages=['Haramlib'],  #same as name
   install_requires=[''], #external packages as dependencies
)
